
상속을 하기 위한 클래스 = 추상 클래스

* 메서드 선언(declaration) {
  int add(int x, int y);
  반환타입 메서드 이름, 매개변수로 구성
  즉 추상적인 메서드, 이것들이 포함된것이 추상 클래스
}

* 메서드 정의 (definition) {
  int add(int x, int y) {
    선언에서 추가적으로 body를 가짐;
  }
}


* 추상 클래스는 new 될 수 없음.

